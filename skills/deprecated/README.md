# Deprecated

No longer used, kept for reference. **Not promoted** — nothing here appears in the top-level `README.md` or in `.claude-plugin/plugin.json`'s `skills` array.

Deprecating rather than deleting keeps the reasoning available; the git history alone rarely explains why something stopped being a good idea. Say why in the skill itself when you move it here.

*Nothing yet.*
