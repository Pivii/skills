# In progress

Drafts not yet ready to ship. **Not promoted** — nothing here appears in the top-level `README.md` or in `.claude-plugin/plugin.json`'s `skills` array, so it is never installed by anyone.

A skill leaves this bucket when it has been held against `/writing-great-skills` and works on a repo that isn't mine.

*Nothing yet.*
